<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.newlecture.web.dao.NoticeDao">
  <select id="getViewList" resultType="com.newlecture.web.entity.NoticeView">
    SELECT * FROM noticeview
    <trim prefix="WHERE" prefixOverrides="AND | OR">
    <if test="query != null and query != ''">
    ${field} LIKE '%${query}%'
    </if>
    </trim>
	ORDER BY id DESC
	LIMIT #{offset}, #{size}
  </select>
  
  <select id="getCount">
  	SELECT count(id) count FROM notice
    WHERE ${field} LIKE '%${query}%'
  </select>
  
  <select id="getView" resultType="com.newlecture.web.entity.NoticeView">
  	SELECT * FROM noticeview
    WHERE id = #{id}
  </select>
  
  <select id="getNext" resultType="com.newlecture.web.entity.Notice">
  	select *  from notice
	where id &gt; (select id from newlecture.notice where id = #{id})
	order by id asc
	limit 1
  </select>
  
  <select id="getPrev" resultType="com.newlecture.web.entity.Notice">
  	select *  from notice
	where id &lt; (select id from newlecture.notice where id = #{id})
	order by id desc
	limit 1
  </select>
  
  <update id="update" resultType="com.newlecture.web.entity.Notice">
  	update notice
  	set
  		title = #{title},
  		content = #{content},
  		pub = #{pub}
  	where id = #{id}
  </update>
  
  <insert id="insert" resultType="com.newlecture.web.entity.Notice">
  	insert into notice (title, content, memberId)
  	values (#{title}, #{content}, #{memberId},)
  </insert>
  
  <delete id="delete">
  	delete from notice
  	where id = #{id}
  </delete>
  
  <delete id="deleteAll">
  	delete from notice
  	where id in
  	<foreach item="id" index="index" collection="ids"
      open="(" separator="," close=")">
        #{id}
  	</foreach>
  </delete>
  
  <update id="updatePubAll">
  	update notice
  	set
  		pub = #{pub}
  	where id in
  	<foreach item="id" index="index" collection="ids"
      open="(" separator="," close=")">
        #{id}
  	</foreach>
  
<!--  	
  	update notice
  	set
  		pub = case id
  			<foreach item="id" collection="pubIds">
  				when #{id} then 1
  			</foreach>
  			<foreach item="id" collection="closeIds">
  				when #{id} then 0
  			</foreach>
  			end
  	where id in (
  		<foreach item="id" collection="pubIds">
  			#{id}
  		</foreach>
  		,
  		<foreach item="id" collection="closeIds">
  			#{id}
  		</foreach>
  	)
-->  		
  </update>
  
</mapper>